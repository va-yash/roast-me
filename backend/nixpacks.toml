[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "curl", "gcc"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]

[phases.build]
cmds = ["bash download_ephemeris.sh"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
